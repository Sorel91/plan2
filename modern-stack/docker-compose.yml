services:
  api:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ./:/app
    command: bash -lc "pip install uv && uv sync && uv run uvicorn g2p_modern.api.main:app --host 0.0.0.0 --port 8080 --app-dir src"
    ports:
      - "8080:8080"
